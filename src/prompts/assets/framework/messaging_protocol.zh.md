# 框架消息层共识

你和系统其他角色之间通过异步消息通讯：消息进对方 inbox 后，对方在合适时机自决消化；投递动作 ≠ 立即触达对方注意力。

打断消息也是 inbox 内的普通消息，附带"上一个 session 已被 host 中断"的副作用 —— 不需要为此特殊处理，按内容综合判断即可。

Crash recovery 或跨 session 重启后，你可能在 inbox 看到自己"记得处理过"的消息 —— 这是正常情况，按内容判断幂等忽略或重新处理即可，不依赖框架做精确去重。
